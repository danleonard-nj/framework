replicaCount: 1

image:
  repository: azureks.azurecr.io/features/feature-api
  pullPolicy: Always
  tag: "latest"

imagePullSecrets:
  - name: regcred

nameOverride: "feature-api"
fullnameOverride: "feature-api"

environment:
  environmentVariables: []
  environmentSecrets:
    - name: az-client-id
      key: AZURE_CLIENT_ID
    - name: az-client-secret
      key: AZURE_CLIENT_SECRET
    - name: az-tenant-id
      key: AZURE_TENANT_ID
    - name: az-storage-connection-string
      key: AzureWebJobsStorage

probes:
  endpoints:
    liveness: "/api/health/alive"
    readiness: "/api/health/ready"
  initialDelay: 15
  interval: 10
  timeout: 30
  
serviceAccount:
  create: true
  name: "feature-api"

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  
resources:
  limits:
    cpu: 200m
    memory: 500Mi
  requests:
    cpu: 125m
    memory: 400Mi
    
autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 85
  targetMemoryUtilizationPercentage: 90